<UserControl x:Class="ClickUpVS.Views.SpaceList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClickUpVS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <TextBlock Text="Spaces"
                   FontWeight="Bold"
                   Margin="10"
                   DockPanel.Dock="Top"/>

        <ListBox x:Name="SpacesListBox"
                 DisplayMemberPath="Name"
                 BorderThickness="1"
                 DockPanel.Dock="Top" HorizontalAlignment="Left" SelectionChanged="SpacesListBox_SelectionChanged"/>
    </DockPanel>
</UserControl>
